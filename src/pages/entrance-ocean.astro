---
import { SITE } from "../data/site";
import { href } from "../lib/url";

const wikiHomeUrl = href("/home");
// 左上角 Logo 指回原版入口（"/"），與該頁的 Logo 互為往返
const originEntranceUrl = href("/");
// 這頁是首頁的另一種皮膚（標題與描述都相同），權重集中回 "/"，避免被判重複內容
const canonicalUrl = new URL(originEntranceUrl, Astro.site).href;
---

<!doctype html>
<html lang="en" class="ocean-html">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover" />
    <meta
      name="description"
      content="Advanced game guides and mechanics for The Legend of Zelda, Super Smash Bros., and beyond."
    />
    <meta name="theme-color" content="#dce5ec" />
    <link rel="icon" href={href("/favicon.svg")} type="image/svg+xml" />
    <link rel="canonical" href={canonicalUrl} />
    <title>Yuda's Channel · Game Wiki</title>

    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Fraunces:opsz,wght@9..144,400;9..144,500&family=Noto+Sans+TC:wght@400;500;600&family=Noto+Serif+JP:wght@400;500&family=Noto+Serif+TC:wght@400;500&family=Spectral:ital,wght@1,400&display=swap"
      rel="stylesheet"
    />

    <script is:inline>
      (function () {
        var theme = "dark";
        try {
          var saved = localStorage.getItem("wiki-theme");
          if (saved === "light" || saved === "dark") {
            theme = saved;
          } else {
            theme = window.matchMedia("(prefers-color-scheme: light)").matches
              ? "light"
              : "dark";
          }
        } catch (error) {
          theme = "dark";
        }
        document.documentElement.setAttribute("data-theme", theme);
        document.documentElement.style.colorScheme = theme;

        var lang = "en";
        try {
          var savedLang = localStorage.getItem("entrance-lang");
          if (savedLang === "zh" || savedLang === "en" || savedLang === "ja") {
            lang = savedLang;
          }
        } catch (error) {
          lang = "en";
        }
        document.documentElement.setAttribute("data-lang", lang);
        document.documentElement.setAttribute(
          "lang",
          lang === "zh" ? "zh-Hant-TW" : lang === "ja" ? "ja" : "en"
        );
      })();
    </script>
  </head>

  <body class="ocean-page">
    <a class="skip-link" href="#ocean-content">Skip to main content</a>

    <div class="scene" aria-hidden="true">
      <picture class="scene-art scene-art-day">
        <source
          media="(max-width: 767px)"
          type="image/avif"
          srcset={href("/entrance-ocean/island-coast-dazotikuy-v2-mobile.avif")}
        />
        <source
          media="(max-width: 767px)"
          type="image/webp"
          srcset={href("/entrance-ocean/island-coast-dazotikuy-v2-mobile.webp")}
        />
        <source
          type="image/avif"
          srcset={href("/entrance-ocean/island-coast-dazotikuy-v2.avif")}
        />
        <img
          src={href("/entrance-ocean/island-coast-dazotikuy-v2.webp")}
          alt=""
          width="1672"
          height="941"
          decoding="async"
          fetchpriority="high"
        />
      </picture>
      <picture class="scene-art scene-art-night">
        <source
          media="(max-width: 767px)"
          type="image/avif"
          srcset={href("/entrance-ocean/island-coast-dazotikuy-v2-night-mobile.avif")}
        />
        <source
          media="(max-width: 767px)"
          type="image/webp"
          srcset={href("/entrance-ocean/island-coast-dazotikuy-v2-night-mobile.webp")}
        />
        <source
          type="image/avif"
          srcset={href("/entrance-ocean/island-coast-dazotikuy-v2-night.avif")}
        />
        <img
          src={href("/entrance-ocean/island-coast-dazotikuy-v2-night.webp")}
          alt=""
          width="1672"
          height="941"
          decoding="async"
        />
      </picture>
      <div class="scene-lighting scene-lighting-day"></div>
      <div class="scene-lighting scene-lighting-night"></div>
      <div class="scene-lighting scene-night-depth"></div>
      <div class="scene-lighting scene-night-moonlight"></div>
      <div class="sky"></div>
      <div class="stars stars-far"></div>
      <div class="stars stars-near"></div>

      <svg class="cloud-layer cloud-layer-far ambient-motion" viewBox="0 0 1600 440" preserveAspectRatio="none">
        <path d="M-90 138C82 95 206 113 337 148c115 31 229 17 356-21 154-47 274-30 390 9 150 51 281 23 433-15 93-23 157-13 216 12v126H-90Z" />
        <path d="M140 47c121-30 238-14 341 15 124 35 238 29 339-3 100-32 198-24 298 9 100 32 190 34 283 8v104H140Z" />
      </svg>
      <svg class="cloud-layer cloud-layer-near ambient-motion" viewBox="0 0 1600 400" preserveAspectRatio="none">
        <path d="M-120 230c146-88 302-69 424-15 108 48 225 59 355 10 130-49 281-47 401 2 127 52 240 52 391-14 90-40 183-26 268 17v120H-120Z" />
      </svg>

      <div class="haze haze-one ambient-motion"></div>
      <div class="haze haze-two ambient-motion"></div>

      <svg class="distant-islands" viewBox="0 0 1600 220" preserveAspectRatio="none">
        <path class="island-far" d="M0 186c128-4 235-9 355-6 93 2 152-7 231-31 47-15 83-8 126 11 39 17 89 23 147 22 85-1 169-23 240-26 109-4 214 25 501 27v37H0Z" />
        <path class="island-mist" d="M0 197c217-17 315-12 476-6 142 5 236-13 354-7 160 9 308 0 486 5 107 4 200 8 284 7v24H0Z" />
      </svg>

      <svg class="main-island" viewBox="0 0 780 390">
        <defs>
          <filter id="island-texture" x="-8%" y="-8%" width="116%" height="116%">
            <feTurbulence type="fractalNoise" baseFrequency="0.009 0.025" numOctaves="2" seed="18" result="noise" />
            <feDisplacementMap in="SourceGraphic" in2="noise" scale="3" />
          </filter>
          <linearGradient id="cliff-light" x1="0" y1="0" x2="0.8" y2="1">
            <stop offset="0" class="cliff-stop-one" />
            <stop offset="0.55" class="cliff-stop-two" />
            <stop offset="1" class="cliff-stop-three" />
          </linearGradient>
          <linearGradient id="grass-light" x1="0" y1="0" x2="1" y2="0.8">
            <stop offset="0" class="grass-stop-one" />
            <stop offset="1" class="grass-stop-two" />
          </linearGradient>
        </defs>

        <g filter="url(#island-texture)">
          <path class="island-back" d="M100 248c57-54 117-91 188-103 66-11 111-52 170-48 63 5 90 62 136 78 59 20 99 26 134 68l21 79H53Z" />
          <path fill="url(#cliff-light)" d="M59 264c79-17 121-62 198-72 61-8 114 2 175-24 47-20 91-17 130 14 37 30 89 28 150 72l29 96H38Z" />
          <path class="cliff-shadow" d="M153 262c56 0 88-31 131-32 41-2 67 22 111 13 52-11 62-42 112-33 42 7 49 39 109 41 31 1 58 14 91 36l27 63H87c23-27 30-63 66-88Z" />
          <path class="cliff-ridge" d="M89 304c81-24 140-9 211-27 61-15 88 4 141-8 63-14 96-4 147 12 47 15 85 15 133 34l12 35H67Z" />
          <path fill="url(#grass-light)" d="M78 263c48-15 90-66 164-76 65-9 112 1 174-27 42-19 83-12 122 12 40 25 92 40 152 70-57-13-104-7-145-28-39-19-72-20-112-4-59 23-103 11-162 18-84 11-106 45-193 57Z" />
          <path class="shore-rock" d="M49 335c92-24 128-17 211-3 82 14 132 1 220-10 83-10 166-4 257 18l9 22H34Z" />
        </g>

        <g class="temple">
          <path class="temple-back" d="M408 150h105l17 67H391Z" />
          <path class="temple-roof" d="m387 151 70-41 73 42-8 13H397Z" />
          <path class="temple-roof-mark" d="m429 143 29-18 29 18Z" />
          <path class="temple-wall" d="M404 160h108v67H404Z" />
          <path class="temple-column" d="M415 162h13v65h-13Zm35 0h14v65h-14Zm36 0h13v65h-13Z" />
          <path class="temple-door" d="M450 184h14v43h-14Z" />
          <path class="temple-light" d="M452 186h10v35h-10Z" />
          <path class="temple-line" d="M397 174h124M397 220h124" />
        </g>

        <g class="trees tree-back">
          <path d="m196 195-14-48 27 40-4-59 19 57 12-46 3 55Z" />
          <path d="m572 213-6-58 22 46 9-59 9 58 27-46-17 64Z" />
          <path d="m326 181-10-47 24 36 5-55 11 51 23-38-13 53Z" />
        </g>
        <g class="trees tree-front">
          <path d="m137 263-17-61 31 47 2-69 16 65 28-48-17 66Z" />
          <path d="m282 239-12-58 27 45 6-63 12 59 27-45-15 63Z" />
          <path d="m547 258-13-69 29 54 8-72 12 68 33-51-21 71Z" />
          <path d="m641 278-15-62 31 47 3-68 15 64 29-43-17 63Z" />
        </g>
        <g class="tree-trunks">
          <path d="M158 247v26m-1-25-13-16m15 17 16-18M304 225v23m0-22-12-13m13 13 14-15M570 241v24m1-22-15-17m16 18 16-20M664 263v21m0-21-12-14m13 14 13-15" />
        </g>
      </svg>

      <div class="sea">
        <div class="sea-depth"></div>
        <div class="light-path ambient-motion"></div>
        <svg class="wave-lines wave-lines-far ambient-motion" viewBox="0 0 1600 180" preserveAspectRatio="none">
          <path d="M-40 41c113-16 185 14 287 1 94-12 169-4 257 7 132 17 252-17 373-9 118 8 224 32 350 13 124-18 245-6 413 1" />
          <path d="M-20 92c105-13 199 14 296 2 137-17 204 13 336 6 123-6 211-26 324-9 119 19 227 10 350-2 108-10 197 9 334 8" />
          <path d="M34 137c118-13 216 11 322 0 127-14 241 16 352 4 116-12 212-20 329-2 132 20 236-11 355-4 77 4 137 11 208 7" />
        </svg>
        <svg class="wave-lines wave-lines-near ambient-motion" viewBox="0 0 1600 260" preserveAspectRatio="none">
          <path d="M-60 71c184-31 269 27 442 4 151-20 260 4 418 17 180 14 300-43 472-22 124 15 224 18 388-8" />
          <path d="M-30 177c142-23 274 28 423 3 157-26 282 26 435 3 178-26 282-5 423 9 138 14 248-16 409-7" />
          <path d="M30 236c170-28 270 14 430-3 151-17 267 29 410 5 167-28 271 5 422 8 135 2 227-12 335-8" />
        </svg>
      </div>

      <svg class="shore" viewBox="0 0 650 260" preserveAspectRatio="none">
        <path class="shore-sand-shadow" d="M-50 253C74 152 163 165 244 182c83 18 154 21 242-4 75-21 119-13 184 11v71H-50Z" />
        <path class="shore-sand" d="M-70 253c125-79 230-66 314-46 86 20 149 17 237-5 76-20 127-11 190 13v45H-70Z" />
        <path class="shore-foam" d="M-44 232c134-53 212-43 298-22 92 23 150 20 238-2 69-17 116-10 170 6" />
      </svg>

      <svg class="birds ambient-motion" viewBox="0 0 90 32">
        <path d="M2 18c6-6 11-6 17 0 6-6 11-6 17 0M49 7c4-4 8-4 12 0 4-4 8-4 12 0" />
      </svg>

      <div class="scene-grain"></div>
      <div class="edge-vignette"></div>
    </div>

    <header class="ocean-header">
      {/* Logo 是兩種入口皮膚的切換鈕，這裡指回原版入口（"/"）。
          用 <a> 而非 <button>，中鍵開新分頁／右鍵複製連結／鍵盤操作都能照常運作。 */}
      <a class="ocean-brand" href={originEntranceUrl}>
        <picture class="brand-logo-pic">
          <source srcset={href("/logo/yuda-logo-blue.webp")} type="image/webp" />
          <img class="brand-logo" src={href("/logo/yuda-logo-blue.png")} alt="" />
        </picture>
        <span class="brand-copy">Dazotikuy Shrine</span>
      </a>

      <div class="lang-switch" id="entrance-lang-switch">
        <button
          class="lang-switch-button"
          id="entrance-lang-button"
          type="button"
          aria-haspopup="true"
          aria-expanded="false"
          aria-controls="entrance-lang-menu"
          aria-label="Change language"
        >
          <svg viewBox="0 0 24 24" fill="none" aria-hidden="true">
            <circle cx="12" cy="12" r="9" stroke="currentColor" stroke-width="1.35" />
            <path d="M3.4 9.5h17.2M3.4 14.5h17.2" stroke="currentColor" stroke-width="1.25" stroke-linecap="round" />
            <path d="M12 3a14.4 14.4 0 0 1 0 18 14.4 14.4 0 0 1 0-18Z" stroke="currentColor" stroke-width="1.25" />
          </svg>
        </button>

        <div class="lang-menu" id="entrance-lang-menu" role="menu" aria-label="Language" hidden>
          <button class="lang-option" type="button" role="menuitemradio" aria-checked="false" data-lang-option="zh">
            <span>繁體中文</span><span class="lang-check" aria-hidden="true">✓</span>
          </button>
          <button class="lang-option" type="button" role="menuitemradio" aria-checked="false" data-lang-option="en">
            <span>English</span><span class="lang-check" aria-hidden="true">✓</span>
          </button>
          <button class="lang-option" type="button" role="menuitemradio" aria-checked="false" data-lang-option="ja">
            <span>日本語</span><span class="lang-check" aria-hidden="true">✓</span>
          </button>
        </div>
      </div>
    </header>

    <button
      class="celestial-control"
      id="ocean-theme-switch"
      type="button"
      aria-label="Switch to dark theme"
    >
      <span class="celestial-glow" aria-hidden="true"></span>
      <span class="sun-disc" aria-hidden="true">
        <span class="sun-core"></span>
        <span class="sun-rays"></span>
      </span>
      <span class="moon-disc" aria-hidden="true">
        <span class="moon-crater moon-crater-one"></span>
        <span class="moon-crater moon-crater-two"></span>
      </span>
    </button>

    <main id="ocean-content">
      <section class="hero-copy" aria-labelledby="ocean-heading">
        <div class="hero-eyebrow ocean-reveal ocean-reveal-one">
          <span>Yuda's Channel</span>
          <span class="eyebrow-line" aria-hidden="true"></span>
          <span>Game Wiki</span>
        </div>

        <h1 id="ocean-heading" class="hero-title ocean-reveal ocean-reveal-two">
          <span class="hero-title-prefix">Welcome to</span>
          <em>Dazotikuy Shrine</em>
        </h1>

        <p class="hero-support ocean-reveal ocean-reveal-three">
          Advanced game guides and mechanics for The Legend of Zelda, Super Smash Bros., and beyond.
        </p>

        <div class="hero-actions ocean-reveal ocean-reveal-four" aria-label="Entrance actions">
          <a class="hero-action hero-action-primary" href={wikiHomeUrl}>
            <span>Enter Shrine</span>
            <span class="action-icon" aria-hidden="true">
              <svg class="action-default" viewBox="0 0 24 24" fill="none">
                <path d="M6.5 17.5 17.5 6.5M9 6.5h8.5V15" stroke="currentColor" stroke-width="1.45" stroke-linecap="round" stroke-linejoin="round" />
              </svg>
              <img class="action-hover action-favicon" src={href("/favicon.svg")} alt="" />
            </span>
          </a>
          <a
            class="hero-action hero-action-secondary"
            href={SITE.youtube}
            target="_blank"
            rel="noopener noreferrer"
          >
            <span>Visit Channel</span>
            <span class="action-icon" aria-hidden="true">
              <svg class="action-default" viewBox="0 0 24 24" fill="none">
                <path d="M6.5 12h11M13.5 8l4 4-4 4" stroke="currentColor" stroke-width="1.45" stroke-linecap="round" stroke-linejoin="round" />
              </svg>
              <svg class="action-hover action-youtube" viewBox="0 0 24 24" fill="currentColor">
                <path d="M23.5 6.2a3 3 0 0 0-2.1-2.1c-1.9-.5-9.4-.5-9.4-.5s-7.5 0-9.4.5A3 3 0 0 0 .5 6.2C0 8.1 0 12 0 12s0 3.9.5 5.8a3 3 0 0 0 2.1 2.1c1.9.5 9.4.5 9.4.5s7.5 0 9.4-.5a3 3 0 0 0 2.1-2.1c.5-1.9.5-5.8.5-5.8s0-3.9-.5-5.8ZM9.5 15.6V8.4l6.3 3.6-6.3 3.6Z" />
              </svg>
            </span>
          </a>
        </div>
      </section>
    </main>

    <footer class="ocean-footer ocean-reveal ocean-reveal-five">
      <p>© 2026 Yuhudaddy - Content Curated from Personal YouTube Channel</p>
    </footer>

    <style is:global>
      :root,
      :root[data-theme="light"] {
        color-scheme: light;
        --sky-top: oklch(84% 0.055 240);
        --sky-mid: oklch(88% 0.06 278);
        --sky-low: oklch(91% 0.065 38);
        --sky-horizon: oklch(94% 0.035 76);
        --cloud-far: oklch(97% 0.025 273 / 0.44);
        --cloud-near: oklch(98% 0.025 55 / 0.34);
        --haze: oklch(96% 0.035 67 / 0.52);
        --island-far: oklch(57% 0.045 271);
        --island-mist: oklch(70% 0.035 243);
        --island-back: oklch(41% 0.052 267);
        --cliff-one: oklch(55% 0.052 278);
        --cliff-two: oklch(45% 0.055 258);
        --cliff-three: oklch(34% 0.058 247);
        --cliff-shadow: oklch(32% 0.055 248);
        --cliff-ridge: oklch(38% 0.06 260);
        --grass-one: oklch(49% 0.09 155);
        --grass-two: oklch(36% 0.074 175);
        --shore-rock: oklch(29% 0.05 248);
        --tree-back: oklch(30% 0.064 183);
        --tree-front: oklch(23% 0.068 173);
        --tree-trunk: oklch(28% 0.055 55);
        --temple-wall: oklch(66% 0.035 72);
        --temple-shadow: oklch(38% 0.04 258);
        --temple-roof: oklch(30% 0.045 253);
        --temple-line: oklch(84% 0.025 78 / 0.5);
        --temple-light: oklch(73% 0.11 69 / 0);
        --sea-top: oklch(61% 0.075 212);
        --sea-mid: oklch(47% 0.085 208);
        --sea-bottom: oklch(32% 0.075 212);
        --wave: oklch(88% 0.038 204 / 0.36);
        --wave-near: oklch(90% 0.03 187 / 0.24);
        --reflection: oklch(98% 0.045 84 / 0.75);
        --sand: oklch(76% 0.075 72);
        --sand-shadow: oklch(61% 0.08 66);
        --foam: oklch(96% 0.025 83 / 0.68);
        --text: oklch(24% 0.047 234);
        --text-soft: oklch(35% 0.055 227);
        --text-faint: oklch(43% 0.05 224);
        --hairline: oklch(44% 0.074 210 / 0.44);
        --glass: oklch(96% 0.025 210 / 0.29);
        --glass-strong: oklch(94% 0.035 199 / 0.44);
        --glass-edge: oklch(98% 0.025 160 / 0.66);
        --glass-shadow: oklch(25% 0.05 230 / 0.12);
        --focus: oklch(39% 0.12 208);
        --menu-bg: oklch(94% 0.028 218 / 0.94);
        --menu-active: oklch(84% 0.045 204);
        --title-gradient-start: oklch(22% 0.06 239);
        --title-gradient-mid: oklch(34% 0.075 211);
        --title-gradient-end: oklch(31% 0.055 282);
        --title-glow: oklch(94% 0.028 225 / 0.14);
        --sun-core: oklch(98% 0.035 84);
        --sun-edge: oklch(93% 0.065 75);
        --sun-glow: oklch(97% 0.07 75 / 0.38);
        --moon-core: oklch(96% 0.012 95);
        --moon-edge: oklch(86% 0.025 86);
        --moon-glow: oklch(92% 0.01 240 / 0.24);
        --theme-meta: #dce5ec;
        --scene-x: 0px;
        --scene-y: 0px;
        --celestial-x: 94%;
        --celestial-y: 63svh;
        --celestial-control-size: clamp(4rem, 5.8vw, 5.3rem);
        --sea-start: 70.2%;
        --reflection-width: clamp(5.5rem, 9vw, 10rem);
      }

      :root[data-theme="dark"] {
        color-scheme: dark;
        --celestial-x: 93%;
        --sky-top: oklch(21% 0.052 218);
        --sky-mid: oklch(27% 0.06 214);
        --sky-low: oklch(32% 0.058 209);
        --sky-horizon: oklch(35% 0.052 205);
        --cloud-far: oklch(70% 0.018 211 / 0.14);
        --cloud-near: oklch(76% 0.016 205 / 0.12);
        --haze: oklch(64% 0.025 205 / 0.18);
        --island-far: oklch(29% 0.052 265);
        --island-mist: oklch(34% 0.053 251);
        --island-back: oklch(18% 0.052 254);
        --cliff-one: oklch(28% 0.055 271);
        --cliff-two: oklch(22% 0.052 257);
        --cliff-three: oklch(15% 0.043 244);
        --cliff-shadow: oklch(13% 0.038 240);
        --cliff-ridge: oklch(19% 0.05 259);
        --grass-one: oklch(28% 0.062 177);
        --grass-two: oklch(20% 0.052 187);
        --shore-rock: oklch(12% 0.035 241);
        --tree-back: oklch(18% 0.052 184);
        --tree-front: oklch(12% 0.046 179);
        --tree-trunk: oklch(16% 0.035 55);
        --temple-wall: oklch(37% 0.042 265);
        --temple-shadow: oklch(18% 0.04 257);
        --temple-roof: oklch(13% 0.037 248);
        --temple-line: oklch(69% 0.055 251 / 0.34);
        --temple-light: oklch(80% 0.14 75 / 0.72);
        --sea-top: oklch(35% 0.068 202);
        --sea-mid: oklch(27% 0.061 204);
        --sea-bottom: oklch(16% 0.046 207);
        --wave: oklch(76% 0.046 198 / 0.24);
        --wave-near: oklch(70% 0.045 194 / 0.2);
        --reflection: oklch(90% 0.028 205 / 0.62);
        --sand: oklch(36% 0.058 65);
        --sand-shadow: oklch(23% 0.052 58);
        --foam: oklch(80% 0.04 232 / 0.42);
        --text: oklch(94% 0.025 226);
        --text-soft: oklch(84% 0.035 231);
        --text-faint: oklch(72% 0.045 235);
        --hairline: oklch(76% 0.07 229 / 0.42);
        --glass: oklch(27% 0.046 211 / 0.42);
        --glass-strong: oklch(36% 0.052 207 / 0.52);
        --glass-edge: oklch(80% 0.045 202 / 0.45);
        --glass-shadow: oklch(8% 0.025 216 / 0.28);
        --focus: oklch(81% 0.12 205);
        --menu-bg: oklch(21% 0.047 214 / 0.94);
        --menu-active: oklch(31% 0.058 205);
        --title-gradient-start: oklch(96% 0.012 232);
        --title-gradient-mid: oklch(84% 0.025 221);
        --title-gradient-end: oklch(94% 0.015 88);
        --title-glow: oklch(89% 0.018 235 / 0.18);
        --theme-meta: #0d3d4b;
        /* 夜圖不是日圖的單純後製——月光被另外畫在高空。量測橫式夜圖：天空
           最亮區在原圖高度 26.1%（日圖的夕陽則在 59.6%）。月亮必須對準這裡，
           否則 scene-night-moonlight 會在別處再造一個假光源，而 scene-night-depth
           那圈「中心透明、外圍轉暗」的暗角也會跟著偏移，把上方雲層壓成斑駁。

           兩段式對位：橫式圖以 cover 鋪滿時，畫面比例 ≤16:9 由高度決定縮放，
           畫中 26.1% 就等於畫面 26.1%；比 16:9 更寬時改由寬度決定，光源會相對
           上移，此時 50svh − 13.45vw 才是正確位置。取兩者較小值即可涵蓋兩種情形
           （在 16:9 交界處兩式同值，銜接連續）。

           單位一律用 svh 而非 vh：手機瀏覽器的 vh 以「網址列收起」的大視窗
           為準，但 .scene 與本控制項都是 position:fixed、填的是實際可見區域。
           用 vh 會讓日月掉到海平線以下（實測 63vh 會落在可見區的 88%）。 */
        --celestial-y: min(26svh, calc(50svh - 13.45vw));
        --reflection-width: clamp(6.25rem, 10.5vw, 11.5rem);
      }

      .ocean-html {
        min-width: 320px;
        min-height: 100%;
        overflow: hidden;
        background: var(--sky-top);
      }

      .ocean-page {
        position: relative;
        display: grid;
        min-width: 320px;
        min-height: 100svh;
        margin: 0;
        overflow: hidden;
        grid-template-rows: auto 1fr auto;
        background: var(--sky-top);
        color: var(--text);
        font-family: "Noto Sans TC", "Noto Serif JP", sans-serif;
        font-size: 1rem;
        font-kerning: normal;
        -webkit-font-smoothing: antialiased;
        text-rendering: optimizeLegibility;
      }

      .ocean-page *,
      .ocean-page *::before,
      .ocean-page *::after {
        box-sizing: border-box;
      }

      button,
      a {
        -webkit-tap-highlight-color: transparent;
      }

      button {
        font: inherit;
      }

      .skip-link {
        position: fixed;
        z-index: 100;
        top: 10px;
        left: 10px;
        padding: 10px 15px;
        border: 1px solid var(--glass-edge);
        background: var(--menu-bg);
        color: var(--text);
        text-decoration: none;
        transform: translateY(-160%);
        transition: transform 180ms cubic-bezier(0.22, 1, 0.36, 1);
      }

      .skip-link:focus {
        transform: translateY(0);
      }

      .scene {
        position: fixed;
        z-index: 0;
        inset: 0;
        overflow: hidden;
        background: var(--sky-top);
        pointer-events: none;
        isolation: isolate;
      }

      .scene-art,
      .scene-art img,
      .scene-lighting {
        position: absolute;
        inset: 0;
        width: 100%;
        height: 100%;
      }

      .scene-art {
        display: block;
        overflow: hidden;
      }

      .scene-art-day { z-index: 0; }

      .scene-art-night {
        z-index: 1;
        opacity: 0;
        transition: opacity 900ms cubic-bezier(0.65, 0, 0.35, 1);
      }

      .scene-art img {
        object-fit: cover;
        object-position: right center;
        transition:
          filter 900ms cubic-bezier(0.65, 0, 0.35, 1),
          transform 1200ms cubic-bezier(0.65, 0, 0.35, 1);
      }

      .scene-art-day img {
        filter: brightness(0.99) saturate(0.96) contrast(0.98);
        transform: scale(1.002);
      }

      .scene-art-night img {
        /* 夜圖本身已完成深藍色階與月光，避免再套一層對比／飽和調整。 */
        filter: none;
        transform: scale(1.002);
      }

      :root[data-theme="dark"] .scene-art-day img {
        filter: none;
        transform: scale(1.006);
      }

      :root[data-theme="dark"] .scene-art-night { opacity: 1; }

      .scene-lighting {
        z-index: 2;
        pointer-events: none;
        transition: opacity 900ms cubic-bezier(0.65, 0, 0.35, 1);
      }

      .scene-lighting-day {
        background:
          radial-gradient(circle at var(--celestial-x) var(--celestial-y), oklch(98% 0.042 70 / 0.18) 0, oklch(97% 0.038 69 / 0.085) 8%, transparent 17%),
          linear-gradient(90deg, oklch(92% 0.025 264 / 0.08) 0, transparent 48%);
        mix-blend-mode: screen;
        opacity: 1;
      }

      .scene-lighting-night {
        background: linear-gradient(
          180deg,
          oklch(20% 0.06 219 / 0.96) 0%,
          oklch(27% 0.07 214 / 0.92) 48%,
          oklch(33% 0.065 207 / 0.88) 69%,
          oklch(18% 0.052 204 / 0.94) 100%
        );
        mix-blend-mode: color;
        opacity: 0;
      }

      .scene-night-depth {
        background:
          radial-gradient(ellipse at var(--celestial-x) var(--celestial-y), transparent 0 12%, oklch(8% 0.035 218 / 0.06) 34%, oklch(7% 0.038 213 / 0.22) 76%),
          linear-gradient(180deg, oklch(7% 0.04 221 / 0.12) 0%, transparent 42%, oklch(6% 0.04 202 / 0.46) 100%),
          linear-gradient(90deg, oklch(6% 0.04 218 / 0.2), transparent 55%);
        mix-blend-mode: multiply;
        opacity: 0;
      }

      .scene-night-moonlight {
        background:
          radial-gradient(circle at var(--celestial-x) var(--celestial-y), oklch(98% 0.006 210 / 0.25) 0, oklch(89% 0.014 207 / 0.11) 8%, transparent 19%),
          radial-gradient(ellipse at var(--celestial-x) var(--celestial-y), oklch(92% 0.012 208 / 0.11) 0, transparent 29%),
          radial-gradient(ellipse at var(--celestial-x) var(--sea-start), oklch(92% 0.014 202 / 0.13) 0, oklch(72% 0.03 200 / 0.055) 9%, transparent 20%);
        mix-blend-mode: screen;
        opacity: 0;
      }

      :root[data-theme="dark"] .scene-lighting-day { opacity: 0; }
      :root[data-theme="dark"] .scene-lighting-night { opacity: 0.08; }
      :root[data-theme="dark"] .scene-night-depth { opacity: 0.1; }
      :root[data-theme="dark"] .scene-night-moonlight { opacity: 0.24; }

      /* The former vector environment remains as a structural fallback in the
         document, while the painterly game-environment art provides the scene.
         The cloud and haze layers are re-enabled as heavily blurred drifting
         mist veils over the painting — their vector edges disappear under the
         blur, leaving only soft moving light. */
      .sky,
      .distant-islands,
      .main-island,
      .shore {
        display: none;
      }

      .sky {
        position: absolute;
        inset: 0 0 31.5%;
        background:
          radial-gradient(circle at var(--celestial-x) var(--celestial-y), var(--sun-glow) 0, transparent 17%),
          linear-gradient(to bottom, var(--sky-top) 0, var(--sky-mid) 42%, var(--sky-low) 75%, var(--sky-horizon) 100%);
      }

      .sky,
      .stars,
      .cloud-layer,
      .haze,
      .distant-islands path,
      .main-island path,
      .main-island stop,
      .sea,
      .sea-depth,
      .light-path,
      .wave-lines path,
      .shore path,
      .edge-vignette,
      .celestial-control,
      .hero-action,
      .lang-switch-button,
      .lang-menu {
        transition-duration: 900ms;
        transition-timing-function: cubic-bezier(0.65, 0, 0.35, 1);
      }

      .sky,
      .haze,
      .sea,
      .sea-depth,
      .light-path,
      .edge-vignette,
      .hero-action,
      .lang-switch-button,
      .lang-menu {
        transition-property: background-color, background-image, border-color, color, opacity, box-shadow;
      }

      .stars {
        position: absolute;
        z-index: 2;
        inset: 0 0 30%;
        opacity: 0;
        transition-property: opacity;
      }

      .stars-far {
        background-image:
          radial-gradient(circle at 9% 16%, oklch(94% 0.02 245) 0 1px, transparent 1.4px),
          radial-gradient(circle at 20% 32%, oklch(92% 0.04 238) 0 1px, transparent 1.4px),
          radial-gradient(circle at 34% 12%, oklch(96% 0.025 246) 0 1px, transparent 1.3px),
          radial-gradient(circle at 48% 28%, oklch(92% 0.04 232) 0 1px, transparent 1.3px),
          radial-gradient(circle at 61% 9%, oklch(96% 0.025 246) 0 1px, transparent 1.3px),
          radial-gradient(circle at 73% 36%, oklch(93% 0.035 239) 0 1px, transparent 1.3px),
          radial-gradient(circle at 91% 12%, oklch(96% 0.025 246) 0 1px, transparent 1.3px);
      }

      .stars-near {
        background-image:
          radial-gradient(circle at 14% 43%, oklch(97% 0.025 245) 0 1.4px, transparent 1.8px),
          radial-gradient(circle at 30% 24%, oklch(94% 0.04 240) 0 1.2px, transparent 1.7px),
          radial-gradient(circle at 55% 18%, oklch(98% 0.02 240) 0 1.3px, transparent 1.8px),
          radial-gradient(circle at 67% 46%, oklch(94% 0.04 240) 0 1.3px, transparent 1.8px),
          radial-gradient(circle at 87% 31%, oklch(98% 0.02 240) 0 1.5px, transparent 2px);
        animation: none;
      }

      :root[data-theme="dark"] .stars {
        opacity: 0.72;
      }

      :root[data-theme="dark"] .stars-near {
        animation: star-breathe 9s ease-in-out infinite alternate;
      }

      /* 遠景星層以不同週期反相呼吸，兩層交錯避免整片同時明滅 */
      :root[data-theme="dark"] .stars-far {
        animation: star-breathe 13s ease-in-out 1.8s infinite alternate-reverse;
      }

      .cloud-layer {
        position: absolute;
        width: 113%;
        left: -8%;
        fill: var(--cloud-far);
        filter: blur(15px);
        transition-property: fill, opacity;
      }

      .cloud-layer-far {
        top: 5%;
        height: 33%;
        opacity: 0.62;
        animation: cloud-drift 58s ease-in-out infinite alternate;
      }

      .cloud-layer-near {
        top: 20%;
        left: -12%;
        width: 122%;
        height: 33%;
        fill: var(--cloud-near);
        filter: blur(23px);
        opacity: 0.44;
        animation: cloud-drift-near 39s ease-in-out infinite alternate;
      }

      /* 夜空雲量收斂：月夜只留隱約流動的雲影 */
      :root[data-theme="dark"] .cloud-layer-far { opacity: 0.5; }
      :root[data-theme="dark"] .cloud-layer-near { opacity: 0.36; }

      .haze {
        position: absolute;
        border-radius: 50%;
        background: var(--haze);
        filter: blur(26px);
        opacity: 0.3;
      }

      .haze-one {
        top: 52%;
        right: -10%;
        width: 58%;
        height: 17%;
        animation: haze-drift 31s ease-in-out infinite alternate;
      }

      .haze-two {
        top: 60%;
        left: -14%;
        width: 60%;
        height: 13%;
        opacity: 0.2;
        animation: haze-drift 43s ease-in-out infinite alternate-reverse;
      }

      .distant-islands {
        position: absolute;
        top: calc(68% - 7.4rem);
        left: 0;
        width: 100%;
        height: 8rem;
      }

      .island-far {
        fill: var(--island-far);
        opacity: 0.55;
        transition-property: fill, opacity;
      }

      .island-mist {
        fill: var(--island-mist);
        opacity: 0.72;
        transition-property: fill, opacity;
      }

      .main-island {
        position: absolute;
        right: -1.5%;
        bottom: 27.5%;
        width: clamp(35rem, 49vw, 57rem);
        height: auto;
        transform: translate3d(var(--scene-x), var(--scene-y), 0);
      }

      .main-island path,
      .main-island stop {
        transition-property: fill, stroke, stop-color, opacity;
      }

      .island-back { fill: var(--island-back); }
      .cliff-stop-one { stop-color: var(--cliff-one); }
      .cliff-stop-two { stop-color: var(--cliff-two); }
      .cliff-stop-three { stop-color: var(--cliff-three); }
      .grass-stop-one { stop-color: var(--grass-one); }
      .grass-stop-two { stop-color: var(--grass-two); }
      .cliff-shadow { fill: var(--cliff-shadow); opacity: 0.86; }
      .cliff-ridge { fill: var(--cliff-ridge); opacity: 0.73; }
      .shore-rock { fill: var(--shore-rock); }
      .temple-back { fill: var(--temple-shadow); }
      .temple-roof { fill: var(--temple-roof); }
      .temple-roof-mark { fill: var(--temple-wall); opacity: 0.5; }
      .temple-wall { fill: var(--temple-wall); }
      .temple-column { fill: var(--temple-shadow); opacity: 0.68; }
      .temple-door { fill: var(--temple-shadow); }
      .temple-light { fill: var(--temple-light); }
      .temple-line { fill: none; stroke: var(--temple-line); stroke-width: 2; }
      .tree-back { fill: var(--tree-back); opacity: 0.86; }
      .tree-front { fill: var(--tree-front); }
      .tree-trunks { fill: none; stroke: var(--tree-trunk); stroke-width: 5; stroke-linecap: round; }

      .sea {
        position: absolute;
        z-index: 2;
        inset: var(--sea-start) 0 0;
        overflow: hidden;
        background: transparent;
        opacity: 0.4;
      }

      .sea::before {
        position: absolute;
        content: "";
        inset: 0 -2%;
        border-top: 1px solid color-mix(in oklch, var(--wave) 28%, transparent);
        background:
          repeating-radial-gradient(ellipse at 42% 7%, transparent 0 19px, color-mix(in oklch, var(--wave) 20%, transparent) 20px, transparent 22px 48px);
        opacity: 0.22;
        animation: sea-current 23s ease-in-out infinite alternate;
      }

      .sea-depth {
        position: absolute;
        inset: 0;
        background:
          radial-gradient(ellipse at var(--celestial-x) 0%, color-mix(in oklch, var(--reflection) 15%, transparent) 0, transparent 14%),
          linear-gradient(to bottom, transparent 0, color-mix(in oklch, var(--sea-bottom) 12%, transparent) 100%);
      }

      .light-path {
        position: absolute;
        top: -6%;
        left: var(--celestial-x);
        width: var(--reflection-width);
        height: 112%;
        background:
          repeating-linear-gradient(180deg, transparent 0 0.65rem, color-mix(in oklch, var(--reflection) 32%, transparent) 0.72rem, transparent 0.88rem 1.45rem),
          radial-gradient(ellipse at 50% 3%, var(--reflection), transparent 70%);
        clip-path: polygon(45% 0, 55% 0, 94% 100%, 6% 100%);
        -webkit-mask-image: linear-gradient(to bottom, #000 0, rgb(0 0 0 / 0.66) 44%, transparent 94%);
        mask-image: linear-gradient(to bottom, #000 0, rgb(0 0 0 / 0.66) 44%, transparent 94%);
        opacity: 0.18;
        mix-blend-mode: screen;
        transform: translateX(-50%);
        animation: water-light 9.5s ease-in-out infinite alternate;
      }

      /* 水路上的碎光：與主光路反相位錯動，模擬波面glitter */
      .light-path::after {
        content: "";
        position: absolute;
        inset: -3% 10%;
        background:
          repeating-linear-gradient(180deg, transparent 0 1.05rem, color-mix(in oklch, var(--reflection) 52%, transparent) 1.15rem, transparent 1.35rem 2.2rem);
        opacity: 0.4;
        animation: light-glint 6.5s ease-in-out infinite alternate;
      }

      :root[data-theme="dark"] .sea {
        opacity: 0.34;
      }

      :root[data-theme="dark"] .light-path {
        background:
          repeating-linear-gradient(180deg, transparent 0 0.72rem, oklch(94% 0.006 240 / 0.11) 0.8rem, transparent 0.96rem 1.62rem),
          radial-gradient(ellipse at 50% 3%, oklch(94% 0.006 240 / 0.26), oklch(72% 0.02 240 / 0.08) 28%, transparent 70%);
        animation-name: water-light-night;
      }

      :root[data-theme="dark"] .light-path::after {
        background:
          repeating-linear-gradient(180deg, transparent 0 1.05rem, oklch(94% 0.006 240 / 0.2) 1.15rem, transparent 1.35rem 2.2rem);
        opacity: 0.18;
        animation-name: light-glint-night;
      }

      .wave-lines {
        position: absolute;
        left: -4%;
        width: 108%;
        fill: none;
        stroke-linecap: round;
      }

      .wave-lines path { transition-property: stroke; }

      .wave-lines-far {
        top: -3%;
        height: 47%;
        stroke: var(--wave);
        stroke-width: 1.6;
        opacity: 0.2;
        animation: sea-shift 16s ease-in-out infinite alternate;
      }

      .wave-lines-near {
        bottom: -4%;
        height: 76%;
        stroke: var(--wave-near);
        stroke-width: 2.2;
        opacity: 0.14;
        animation: sea-shift-reverse 11s ease-in-out infinite alternate;
      }

      .shore {
        position: absolute;
        z-index: 2;
        bottom: -2%;
        left: -4%;
        width: min(38rem, 39vw);
        height: 27%;
      }

      .shore path { transition-property: fill, stroke; }
      .shore-sand-shadow { fill: var(--sand-shadow); }
      .shore-sand { fill: var(--sand); }
      .shore-foam { fill: none; stroke: var(--foam); stroke-width: 6; stroke-linecap: round; }

      .birds {
        position: absolute;
        top: 26%;
        left: -7rem;
        width: 5.5rem;
        fill: none;
        stroke: var(--text-faint);
        stroke-width: 1.2;
        stroke-linecap: round;
        opacity: 0.48;
        animation: birds-cross 38s linear infinite;
        transition: opacity 520ms cubic-bezier(0.22, 1, 0.36, 1);
      }

      /* 翅膀撲動：以路徑自身包圍盒為原點做垂直壓縮，配合橫越動畫呈現拍翅 */
      .birds path {
        transform-box: fill-box;
        transform-origin: 50% 50%;
        animation: wing-flap 0.85s ease-in-out infinite alternate;
      }

      :root[data-theme="dark"] .birds {
        opacity: 0;
        animation: none;
      }

      :root[data-theme="dark"] .birds path {
        animation: none;
      }

      .scene-grain {
        position: absolute;
        z-index: 4;
        inset: 0;
        background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 160 160' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='n'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='.78' numOctaves='3' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23n)' opacity='.3'/%3E%3C/svg%3E");
        opacity: 0.026;
        mix-blend-mode: soft-light;
      }

      .edge-vignette {
        position: absolute;
        z-index: 3;
        inset: 0;
        background:
          linear-gradient(90deg, color-mix(in oklch, var(--sky-top) 18%, transparent) 0, transparent 47%),
          linear-gradient(to bottom, transparent 68%, color-mix(in oklch, var(--sea-bottom) 18%, transparent) 100%);
      }

      .ocean-header {
        position: relative;
        z-index: 20;
        display: flex;
        align-items: center;
        justify-content: space-between;
        /* 與 index 的 .entrance-header 用同一組間距，兩頁互相切換時
           左上角 Logo 不會位移。 */
        padding:
          max(clamp(24px, 4.4vh, 46px), env(safe-area-inset-top))
          max(clamp(24px, 4.7vw, 68px), env(safe-area-inset-right))
          0
          max(clamp(24px, 4.7vw, 68px), env(safe-area-inset-left));
        pointer-events: none;
      }

      @media (max-width: 720px) {
        .ocean-header {
          padding-top: max(20px, env(safe-area-inset-top));
          padding-inline: max(20px, env(safe-area-inset-left));
        }
      }

      .ocean-brand,
      .lang-switch {
        pointer-events: auto;
      }

      /* Logo 與品牌字樣的尺寸／字型刻意與粒子入口頁（index）對齊，
         兩頁互相切換時左上角不該有任何跳動。 */
      .ocean-brand {
        display: flex;
        min-width: 0;
        align-items: center;
        gap: 11px;
        color: var(--text);
        text-decoration: none;
      }

      .ocean-brand:focus-visible {
        outline: 2px solid var(--focus);
        outline-offset: 6px;
        border-radius: 6px;
      }

      /* <picture> 才是 flex 項目，尺寸與不縮放的規則掛在它身上。
         40px／720px 以下 36px，與 index 的斷點完全相同。 */
      .brand-logo-pic {
        display: block;
        width: 40px;
        height: 40px;
        flex: 0 0 auto;
      }

      @media (max-width: 720px) {
        .brand-logo-pic {
          width: 36px;
          height: 36px;
        }
      }

      .brand-logo {
        display: block;
        width: 100%;
        height: 100%;
        object-fit: contain;
        filter: drop-shadow(0 2px 7px color-mix(in oklch, var(--glass-shadow) 65%, transparent));
        transition: transform 220ms cubic-bezier(0.25, 1, 0.5, 1);
      }

      @media (hover: hover) {
        .ocean-brand:hover .brand-logo {
          transform: scale(1.06);
        }
      }

      /* 字型／字級／字距與 index 的 .brand-copy span 一致；
         overflow 三件組是這頁 header 較窄時的截斷保險，index 沒有對應設定。 */
      .brand-copy {
        overflow: hidden;
        font-family: "Spectral", Georgia, serif;
        font-size: 0.76rem;
        font-style: italic;
        letter-spacing: 0.02em;
        text-overflow: ellipsis;
        white-space: nowrap;
      }

      .lang-switch { position: relative; }

      .lang-switch-button {
        position: relative;
        display: grid;
        width: 2.75rem;
        height: 2.75rem;
        padding: 0;
        place-items: center;
        border: 0;
        border-radius: 50%;
        background: transparent;
        color: var(--text);
        cursor: pointer;
        transition: color 200ms cubic-bezier(0.25, 1, 0.5, 1);
      }

      .lang-switch-button::after {
        position: absolute;
        inset: 0.22rem;
        z-index: 0;
        content: "";
        border: 1px solid color-mix(in oklch, var(--glass-edge) 70%, transparent);
        border-radius: 50%;
        background: color-mix(in oklch, var(--glass) 60%, transparent);
        box-shadow: inset 0 1px 0 color-mix(in oklch, var(--glass-edge) 55%, transparent);
        opacity: 0;
        pointer-events: none;
        transform: scale(0.72);
        transition:
          opacity 220ms cubic-bezier(0.25, 1, 0.5, 1),
          transform 220ms cubic-bezier(0.25, 1, 0.5, 1),
          background-color 900ms cubic-bezier(0.65, 0, 0.35, 1),
          border-color 900ms cubic-bezier(0.65, 0, 0.35, 1);
      }

      .lang-switch-button svg {
        position: relative;
        z-index: 1;
        width: 1.15rem;
        height: 1.15rem;
      }

      .lang-switch-button[aria-expanded="true"]::after {
        opacity: 1;
        transform: scale(1);
      }

      .lang-switch-button:focus-visible,
      .lang-option:focus-visible,
      .hero-action:focus-visible,
      .celestial-control:focus-visible {
        outline: 2px solid var(--focus);
        outline-offset: 4px;
      }

      .lang-menu {
        position: absolute;
        top: calc(100% + 0.65rem);
        right: 0;
        display: grid;
        width: 10.6rem;
        padding: 0.4rem;
        border: 1px solid var(--glass-edge);
        background: var(--menu-bg);
        box-shadow: 0 1rem 2.8rem var(--glass-shadow);
        clip-path: polygon(0.55rem 0, 100% 0, 100% calc(100% - 0.55rem), calc(100% - 0.55rem) 100%, 0 100%, 0 0.55rem);
        transform-origin: top right;
        animation: menu-enter 220ms cubic-bezier(0.22, 1, 0.36, 1) both;
      }

      .lang-menu[hidden] { display: none !important; }

      .lang-option {
        display: flex;
        min-height: 2.65rem;
        align-items: center;
        justify-content: space-between;
        padding: 0 0.75rem;
        border: 0;
        background: transparent;
        color: var(--text-soft);
        font-size: 0.82rem;
        letter-spacing: 0.01em;
        text-align: left;
        cursor: pointer;
        transition: background-color 160ms cubic-bezier(0.25, 1, 0.5, 1), color 160ms;
      }

      .lang-option[aria-checked="true"] {
        background: var(--menu-active);
        color: var(--text);
      }

      .lang-check { opacity: 0; }
      .lang-option[aria-checked="true"] .lang-check { opacity: 1; }

      .celestial-control {
        position: fixed;
        z-index: 9;
        top: var(--celestial-y);
        left: var(--celestial-x);
        width: var(--celestial-control-size);
        height: var(--celestial-control-size);
        padding: 0;
        border: 0;
        border-radius: 50%;
        background: transparent;
        cursor: pointer;
        transform: translate(-50%, -50%);
        transition-property: opacity;
      }

      .celestial-control.is-switching {
        pointer-events: none;
      }

      .celestial-control.is-relocating {
        pointer-events: none;
      }

      .celestial-glow,
      .sun-disc,
      .moon-disc {
        position: absolute;
        inset: 0;
        border-radius: 50%;
        transition: opacity 650ms cubic-bezier(0.65, 0, 0.35, 1), background 900ms cubic-bezier(0.65, 0, 0.35, 1), box-shadow 900ms cubic-bezier(0.65, 0, 0.35, 1);
      }

      .celestial-control.is-switching .celestial-glow,
      .celestial-control.is-switching .sun-disc,
      .celestial-control.is-switching .moon-disc {
        opacity: 0;
        transition-duration: 260ms;
      }

      .celestial-glow {
        inset: -82%;
        background: radial-gradient(circle, var(--sun-glow), transparent 70%);
        opacity: 0.3;
        filter: blur(0.22rem);
        mix-blend-mode: screen;
        animation: celestial-breathe 8s ease-in-out infinite alternate;
      }

      .sun-disc {
        inset: 32%;
        opacity: 1;
      }

      .sun-core {
        position: absolute;
        inset: 0;
        border-radius: 50%;
        background:
          radial-gradient(circle at 38% 32%, oklch(100% 0.008 76 / 0.74) 0 10%, transparent 36%),
          radial-gradient(circle at 42% 38%, oklch(100% 0.012 76), var(--sun-core) 58%, var(--sun-edge) 100%);
        box-shadow:
          0 0 0.65rem color-mix(in oklch, var(--sun-core) 46%, transparent),
          0 0 1.8rem color-mix(in oklch, var(--sun-glow) 68%, transparent);
        /* 外緣以 alpha 漸層淡出，讓圓盤溶進自身光暈與背景天空，
           不再是一個貼上去的硬邊圓形。 */
        -webkit-mask-image: radial-gradient(circle, #000 54%, rgb(0 0 0 / 0.5) 78%, transparent 100%);
        mask-image: radial-gradient(circle, #000 54%, rgb(0 0 0 / 0.5) 78%, transparent 100%);
      }

      .sun-rays {
        display: none;
      }

      .moon-disc {
        inset: 31%;
        opacity: 0;
        transform: rotate(-12deg) scale(1);
        background: radial-gradient(circle at 30% 36%, oklch(98% 0.006 95), var(--moon-core) 58%, var(--moon-edge) 100%);
        /* 只保留彎月本體；月光暈由 celestial-glow 獨立繪製，
           避免 drop-shadow 沿遮罩邊緣形成一圈細線。 */
        filter: none;
        /* 第一層挖出新月缺口，外圈維持實心邊界，避免半透明外緣
           在彎月右上角留下一圈不自然的輪廓。 */
        -webkit-mask-image:
          radial-gradient(circle at 73% 37%, transparent 0 50%, #000 53%, #000 100%);
        mask-image:
          radial-gradient(circle at 73% 37%, transparent 0 50%, #000 53%, #000 100%);
      }

      .moon-disc::after {
        display: none;
      }

      .moon-crater {
        display: none;
      }

      .moon-crater-one { left: 23%; top: 51%; width: 17%; height: 17%; }
      .moon-crater-two { left: 44%; top: 72%; width: 10%; height: 10%; }

      :root[data-theme="dark"] .celestial-glow {
        background: radial-gradient(circle, var(--moon-glow), transparent 70%);
        opacity: 0.28;
      }

      :root[data-theme="dark"] .sun-disc {
        opacity: 0;
      }

      :root[data-theme="dark"] .moon-disc {
        opacity: 1;
      }

      #ocean-content {
        position: relative;
        z-index: 5;
        min-height: 0;
      }

      .hero-copy {
        position: absolute;
        top: clamp(8.8rem, 30vh, 17rem);
        left: max(1.35rem, env(safe-area-inset-left));
        width: min(calc(100vw - 2.7rem), 35rem);
        color: var(--text);
        text-align: left;
        filter: drop-shadow(0 1px 0 color-mix(in oklch, var(--sky-horizon) 40%, transparent));
      }

      .hero-copy::before {
        display: none;
      }

      .hero-eyebrow {
        display: flex;
        align-items: center;
        gap: 0.9rem;
        color: var(--text-faint);
        font-size: clamp(0.65rem, 1.8vw, 0.75rem);
        font-weight: 600;
        letter-spacing: 0.13em;
        text-transform: uppercase;
        white-space: nowrap;
      }

      .eyebrow-line {
        width: clamp(1.8rem, 7vw, 4.4rem);
        height: 1px;
        background: linear-gradient(90deg, var(--hairline), transparent);
      }

      .hero-title {
        display: flex;
        flex-direction: column;
        margin: 1.2rem 0 0;
        font-family: "Fraunces", "Noto Serif TC", "Noto Serif JP", serif;
        font-size: clamp(2.55rem, 11.4vw, 4.8rem);
        font-variation-settings: "opsz" 100;
        font-weight: 400;
        letter-spacing: -0.045em;
        line-height: 1.08;
      }

      .hero-title-prefix {
        margin-bottom: 0.72rem;
        font-size: 0.36em;
        font-style: normal;
        letter-spacing: 0.015em;
        line-height: 1.15;
      }

      .hero-title em {
        display: inline-block;
        width: max-content;
        max-width: none;
        padding-inline-end: 0.16em;
        background: linear-gradient(
          108deg,
          var(--title-gradient-start) 0%,
          var(--title-gradient-mid) 52%,
          var(--title-gradient-end) 100%
        );
        background-clip: text;
        -webkit-background-clip: text;
        color: var(--text);
        font-style: italic;
        font-weight: 500;
        line-height: 1.08;
        -webkit-text-fill-color: transparent;
        white-space: nowrap;
        padding: 0 0.2em 0.14em 0;
        margin-bottom: -0.14em;
        filter: drop-shadow(0 0.08em 0.42em var(--title-glow));
        transition: background-image 900ms cubic-bezier(0.65, 0, 0.35, 1), filter 900ms cubic-bezier(0.65, 0, 0.35, 1);
      }

      :root[data-lang="zh"] .hero-title,
      :root[data-lang="ja"] .hero-title {
        font-family: "Noto Serif TC", "Noto Serif JP", serif;
        letter-spacing: 0.01em;
      }

      :root[data-lang="zh"] .hero-title em,
      :root[data-lang="ja"] .hero-title em {
        font-style: italic;
        font-weight: 500;
      }

      :root[data-lang="ja"] .hero-title {
        font-family: "Noto Serif JP", "Noto Serif TC", serif;
      }

      :root[data-lang="ja"] .hero-title em { order: 1; }
      :root[data-lang="ja"] .hero-title-prefix { order: 2; margin: 0.6rem 0 0; }

      .hero-support {
        max-width: 34rem;
        margin: 2rem 0 0;
        color: var(--text-soft);
        font-size: clamp(0.755rem, 2.8vw, 0.875rem);
        line-height: 1.7;
        text-wrap: balance;
      }

      .hero-actions {
        display: flex;
        flex-wrap: wrap;
        gap: 0.65rem;
        margin-top: 2.3rem;
      }

      .hero-action {
        position: relative;
        display: inline-flex;
        min-width: 8.75rem;
        min-height: 3.25rem;
        align-items: center;
        justify-content: space-between;
        gap: 0.85rem;
        overflow: hidden;
        padding: 0.43rem 0.48rem 0.43rem 1rem;
        border: 1px solid var(--glass-edge);
        border-radius: 999px;
        background:
          linear-gradient(
            116deg,
            color-mix(in oklch, var(--glass-edge) 24%, transparent) 0%,
            transparent 28%,
            transparent 72%,
            color-mix(in oklch, var(--glass-edge) 31%, transparent) 100%
          ),
          color-mix(in oklch, var(--glass) 88%, var(--sky-horizon));
        color: var(--text);
        box-shadow:
          inset 0 1px 0 color-mix(in oklch, var(--glass-edge) 72%, transparent),
          inset 0 -1px 0 color-mix(in oklch, var(--hairline) 32%, transparent),
          0 0.75rem 2rem var(--glass-shadow);
        font-size: 0.72rem;
        font-weight: 600;
        letter-spacing: 0.045em;
        text-decoration: none;
        -webkit-backdrop-filter: blur(10px) saturate(125%);
        backdrop-filter: blur(10px) saturate(125%);
        transition-property: background-color, border-color, color, box-shadow;
      }

      .hero-action > span {
        position: relative;
        z-index: 1;
      }

      .hero-action::before {
        position: absolute;
        z-index: 0;
        top: -120%;
        left: -70%;
        width: 55%;
        height: 340%;
        content: "";
        background: linear-gradient(
          90deg,
          transparent 0 28%,
          color-mix(in oklch, var(--glass-edge) 74%, transparent) 48%,
          color-mix(in oklch, var(--glass-edge) 45%, transparent) 55%,
          transparent 78%
        );
        filter: blur(1px);
        opacity: 0.28;
        pointer-events: none;
        transform: rotate(13deg) translateX(-46%);
        transition: opacity 220ms linear, transform 820ms cubic-bezier(0.16, 1, 0.3, 1);
      }

      .hero-action-primary {
        background:
          linear-gradient(116deg, color-mix(in oklch, var(--glass-edge) 31%, transparent), transparent 31% 70%, color-mix(in oklch, var(--glass-edge) 38%, transparent)),
          color-mix(in oklch, var(--glass-strong) 82%, var(--sky-horizon));
      }

      .action-icon {
        position: relative;
        display: grid;
        width: 2.25rem;
        height: 2.25rem;
        flex: 0 0 2.25rem;
        place-items: center;
        overflow: hidden;
        border: 1px solid color-mix(in oklch, var(--glass-edge) 76%, transparent);
        border-radius: 50%;
        background: color-mix(in oklch, var(--glass) 84%, transparent);
      }

      .action-icon svg,
      .action-icon img {
        position: absolute;
        top: 50%;
        left: 50%;
        width: 1rem;
        height: 1rem;
        transform: translate(-50%, -50%);
        transition: opacity 180ms cubic-bezier(0.25, 1, 0.5, 1), transform 240ms cubic-bezier(0.25, 1, 0.5, 1);
      }

      .action-hover {
        opacity: 0;
        transform: translate(-50%, -50%) scale(0.66) rotate(-10deg);
      }
      .action-favicon { object-fit: contain; }
      .action-youtube { color: #c94a3d; }

      .hero-action:active {
        background: color-mix(in oklch, var(--glass-strong) 78%, var(--sea-mid));
        box-shadow: inset 0 2px 5px color-mix(in oklch, var(--sea-bottom) 36%, transparent);
      }

      .hero-action:active::before {
        opacity: 0.92;
        transform: rotate(13deg) translateX(390%);
      }

      .hero-action:active .action-default {
        opacity: 0;
        transform: translate(-50%, -50%) scale(0.68) rotate(10deg);
      }

      .hero-action:active .action-hover {
        opacity: 1;
        transform: translate(-50%, -50%) scale(1) rotate(0);
      }

      .ocean-footer {
        position: relative;
        z-index: 5;
        padding:
          0
          max(1.35rem, env(safe-area-inset-right))
          max(1.1rem, env(safe-area-inset-bottom))
          max(1.35rem, env(safe-area-inset-left));
        color: var(--text-faint);
        font-size: clamp(0.57rem, 1.8vw, 0.68rem);
        letter-spacing: 0.02em;
        line-height: 1.45;
      }

      .ocean-footer p { margin: 0; }

      .ocean-reveal {
        opacity: 0;
        animation: reveal-in 760ms cubic-bezier(0.22, 1, 0.36, 1) forwards;
      }

      .ocean-reveal-one { animation-delay: 100ms; }
      .ocean-reveal-two { animation-delay: 170ms; }
      .ocean-reveal-three { animation-delay: 260ms; }
      .ocean-reveal-four { animation-delay: 350ms; }
      .ocean-reveal-five { animation-delay: 430ms; }

      .ocean-page.is-paused .ambient-motion,
      .ocean-page.is-paused .ambient-motion::after,
      .ocean-page.is-paused .ambient-motion path,
      .ocean-page.is-paused .sea::before,
      .ocean-page.is-paused .celestial-glow,
      .ocean-page.is-paused .stars-near,
      .ocean-page.is-paused .stars-far {
        animation-play-state: paused;
      }

      @media (hover: hover) {
        .lang-switch-button:hover {
          color: var(--text);
        }

        .lang-switch-button:hover::after {
          opacity: 1;
          transform: scale(1);
        }

        .lang-option:hover { background: var(--menu-active); color: var(--text); }

        .hero-action:hover { border-color: color-mix(in oklch, var(--glass-edge) 78%, var(--hairline)); }
        .hero-action:hover::before { opacity: 0.92; transform: rotate(13deg) translateX(390%); }
        .hero-action:hover .action-default { opacity: 0; transform: translate(-50%, -50%) scale(0.68) rotate(10deg); }
        .hero-action:hover .action-hover { opacity: 1; transform: translate(-50%, -50%) scale(1) rotate(0); }

        .celestial-control:hover .sun-disc,
        .celestial-control:hover .moon-disc {
          filter: brightness(1.08);
        }
      }

      @media (min-width: 46rem) {
        .hero-copy {
          top: clamp(15.8rem, 43vh, 25rem);
          left: max(3.5rem, 8vw, env(safe-area-inset-left));
          width: min(45vw, 36rem);
          transform: translateY(-18%);
        }

        .hero-title { font-size: clamp(3.5rem, 5.3vw, 5rem); }

        .ocean-footer {
          padding-right: max(2.3rem, 4vw, env(safe-area-inset-right));
          padding-bottom: max(1.4rem, env(safe-area-inset-bottom));
          padding-left: max(3.5rem, 8vw, env(safe-area-inset-left));
        }
      }

      @media (min-width: 70rem) {
        .hero-actions { gap: 0.85rem; }
        .hero-action { min-width: 9.25rem; min-height: 3.3rem; }
      }

      /* 直式夜圖沿用右上方的月亮位置，讓響應式版也維持與桌面一致的
         夜空視線；原圖右側的雲光本身負責海面附近的自然反射。 */
      @media (max-width: 767px) {
        :root[data-theme="dark"] {
          /* 直式夜圖右側的月光雲層在可視高度約 54%；月亮控制項
             對準這個自然光源，避免浮在上方空無一物的天空。 */
          --celestial-y: 54svh;
        }

        /* 直式夜圖已內建右側月光；手機不再重複疊加整面色層與高光，
           只保留極淡的深度，讓背景維持原圖的自然階調。 */
        :root[data-theme="dark"] .scene-lighting-night { opacity: 0; }
        :root[data-theme="dark"] .scene-night-depth { opacity: 0.04; }
        :root[data-theme="dark"] .scene-night-moonlight { opacity: 0.1; }
      }

      @media (max-width: 45.99rem) {
        :root,
        :root[data-theme="light"] {
          --celestial-x: 92%;
          --celestial-y: 63svh;
          --celestial-control-size: 4rem;
          --sea-start: 71.2%;
          --reflection-width: 5rem;
        }

        :root[data-theme="dark"] {
          --reflection-width: 6rem;
        }

        .sky { bottom: 34%; }
        .distant-islands { top: calc(66% - 5.3rem); height: 5.8rem; }

        .main-island {
          right: -23%;
          bottom: 32.5%;
          width: 98vw;
        }

        .shore { width: 63vw; height: 20%; left: -13%; }
        .hero-copy::before { opacity: 0.58; }
        .birds { top: 21%; }

        .hero-action {
          background: color-mix(in oklch, var(--glass-strong) 82%, var(--sky-horizon));
        }

        .hero-actions {
          width: min(60vw, 12.5rem);
          flex-direction: column;
          gap: 0.65rem;
        }

        .hero-action {
          width: 100%;
          min-width: 0;
        }

        /* 中文標題寬約 8.28 個字級（最長的一種），10.3vw 會讓右側只剩約 35px。
           降到 9vw 讓留白回到約 70px，日文共用同一級距以維持 CJK 視覺一致。 */
        :root[data-lang="zh"] .hero-title,
        :root[data-lang="ja"] .hero-title {
          font-size: clamp(1.95rem, 9vw, 2.9rem);
        }

        .ocean-footer {
          color: color-mix(in oklch, var(--text) 78%, var(--text-faint));
          text-shadow: 0 1px 0 color-mix(in oklch, var(--sky-horizon) 65%, transparent);
        }
      }

      @media (max-width: 25rem) {
        .hero-copy { top: clamp(8.1rem, 27vh, 12.5rem); }
        .hero-title { font-size: clamp(2.3rem, 11.2vw, 2.75rem); }
        .hero-support { margin-top: 1.3rem; font-size: 0.7rem; line-height: 1.58; }
        .hero-actions { margin-top: 1.5rem; }
        .hero-action { padding: 0.4rem 0.45rem 0.4rem 1rem; font-size: 0.68rem; }
        .ocean-footer { max-width: 92%; }
      }

      @media (max-height: 43.75rem) {
        :root,
        :root[data-theme="light"] {
          --celestial-y: 63svh;
        }

        :root {
          --sea-start: 74%;
        }

        .hero-copy { top: 7.5rem; }
        /* 這條規則原本只用 8.5vh 決定字級，完全不看寬度，於是在「矮又窄」的
           手機（例如 iPhone 13 可視高度約 643px）會把英文標題撐到 56px 而衝出
           畫面右緣。外面再包一層 min()，讓寬度成為真正的上限。
           11.6vw 是由實測比值反推：英文標題寬約 6.34 個字級，扣掉左緣 22px
           與右側安全距離後仍有餘裕。 */
        .hero-title { font-size: min(clamp(2.15rem, 8.5vh, 3.6rem), 11.6vw); }
        .hero-title-prefix { margin-bottom: 0.35rem; }
        .hero-support { margin-top: 1.1rem; line-height: 1.5; }
        .hero-actions { margin-top: 1.3rem; }
        .main-island { bottom: 24%; }
        .distant-islands { top: calc(74% - 5rem); }
        .shore { height: 18%; }
      }

      @media (max-width: 45.99rem) and (max-height: 43.75rem) {
        :root,
        :root[data-theme="light"] { --celestial-y: 63svh; }

        .hero-copy { top: 6.8rem; }
        .main-island { right: -14%; bottom: 24.5%; width: 78vw; }
      }

      @media (pointer: coarse) {
        .hero-action { min-height: 3rem; }
        .lang-switch-button { width: 3rem; height: 3rem; }
      }

      @media (prefers-reduced-motion: reduce) {
        .ambient-motion,
        .ambient-motion::after,
        .ambient-motion path,
        .sea::before,
        .celestial-glow,
        .stars-near,
        .stars-far,
        .ocean-reveal {
          animation: none !important;
        }

        .ocean-reveal { opacity: 1; }

        .sky,
        .stars,
        .cloud-layer,
        .haze,
        .distant-islands path,
        .main-island path,
        .main-island stop,
        .sea,
        .sea-depth,
        .light-path,
        .wave-lines path,
        .shore path,
        .edge-vignette,
        .celestial-control,
        .celestial-glow,
        .sun-disc,
        .moon-disc,
        .moon-disc::after,
        .hero-action,
        .hero-action::before,
        .action-icon svg,
        .action-icon img,
        .lang-switch-button,
        .lang-menu,
        .skip-link {
          transition-duration: 0.01ms !important;
        }
      }

      @keyframes reveal-in {
        from { opacity: 0; transform: translateY(0.8rem); }
        to { opacity: 1; transform: translateY(0); }
      }

      /* 環境動畫全部只動 transform / opacity（GPU 合成層），
         各層週期取互質的秒數，整個場景不會出現可察覺的同步循環點 */
      @keyframes cloud-drift {
        0% { transform: translate3d(-3%, 0.3rem, 0); }
        55% { transform: translate3d(0.4%, -0.35rem, 0); }
        100% { transform: translate3d(3.2%, 0.15rem, 0); }
      }

      @keyframes cloud-drift-near {
        0% { transform: translate3d(3.6%, -0.2rem, 0); }
        50% { transform: translate3d(-0.6%, 0.4rem, 0); }
        100% { transform: translate3d(-4.4%, -0.25rem, 0); }
      }

      @keyframes haze-drift {
        from { transform: translate3d(-2.3%, 0.2rem, 0); }
        to { transform: translate3d(2.5%, -0.4rem, 0); }
      }

      @keyframes sea-shift {
        0% { transform: translate3d(-1.5%, 0.12rem, 0); opacity: 0.16; }
        50% { transform: translate3d(0.3%, -0.12rem, 0) skewX(0.35deg); opacity: 0.3; }
        100% { transform: translate3d(1.7%, 0.06rem, 0); opacity: 0.2; }
      }

      @keyframes sea-shift-reverse {
        0% { transform: translate3d(1.9%, 0, 0); opacity: 0.12; }
        50% { transform: translate3d(-0.4%, -0.18rem, 0) skewX(-0.4deg); opacity: 0.26; }
        100% { transform: translate3d(-2.3%, 0.1rem, 0); opacity: 0.16; }
      }

      @keyframes sea-current {
        from { transform: translate3d(-0.8%, 0, 0); opacity: 0.16; }
        to { transform: translate3d(0.9%, 0, 0); opacity: 0.3; }
      }

      @keyframes water-light {
        0% { transform: translate3d(-52%, 0, 0) scaleX(0.92); opacity: 0.2; }
        45% { transform: translate3d(-49.5%, 0.1rem, 0) scaleX(1.06); opacity: 0.42; }
        100% { transform: translate3d(-48%, 0, 0) scaleX(0.97); opacity: 0.3; }
      }

      @keyframes light-glint {
        0% { transform: translateY(-0.45rem); opacity: 0.18; }
        60% { transform: translateY(0.15rem); opacity: 0.66; }
        100% { transform: translateY(0.5rem); opacity: 0.34; }
      }

      @keyframes water-light-night {
        0% { transform: translate3d(-52%, 0, 0) scaleX(0.92); opacity: 0.06; }
        45% { transform: translate3d(-49.5%, 0.1rem, 0) scaleX(1.06); opacity: 0.14; }
        100% { transform: translate3d(-48%, 0, 0) scaleX(0.97); opacity: 0.1; }
      }

      @keyframes light-glint-night {
        0% { transform: translateY(-0.45rem); opacity: 0.06; }
        60% { transform: translateY(0.15rem); opacity: 0.18; }
        100% { transform: translateY(0.5rem); opacity: 0.12; }
      }

      @keyframes wing-flap {
        from { transform: scaleY(1); }
        to { transform: scaleY(0.62); }
      }

      @keyframes celestial-breathe {
        from { transform: scale(0.98); opacity: 0.26; }
        to { transform: scale(1.025); opacity: 0.42; }
      }

      @keyframes star-breathe {
        from { opacity: 0.5; }
        to { opacity: 0.85; }
      }

      @keyframes birds-cross {
        0%, 14% { transform: translate3d(-8rem, 0, 0); opacity: 0; }
        18% { opacity: 0.46; }
        52% { opacity: 0.46; }
        58%, 100% { transform: translate3d(calc(100vw + 10rem), -1.8rem, 0); opacity: 0; }
      }

      @keyframes menu-enter {
        from { opacity: 0; transform: translateY(-0.35rem); }
        to { opacity: 1; transform: translateY(0); }
      }
    </style>

    <script is:inline>
      (function () {
        var root = document.documentElement;
        var page = document.querySelector(".ocean-page");
        var themeButton = document.getElementById("ocean-theme-switch");
        var themeMeta = document.querySelector('meta[name="theme-color"]');
        var descriptionMeta = document.querySelector('meta[name="description"]');

        document.addEventListener("touchstart", function () {}, { passive: true });

        var I18N = {
          zh: {
            htmlLang: "zh-Hant-TW",
            docTitle: "Yuda 的頻道・遊戲攻略 Wiki",
            metaDescription: "收錄薩爾達傳說、大亂鬥特別版等系列的進階攻略技巧與程錯解說。",
            skip: "跳到主要內容",
            brand: "Dazotikuy Shrine",
            eyebrowLeft: "Yuda 的頻道",
            eyebrowRight: "遊戲攻略 Wiki",
            titleLead: "歡迎來到",
            titleBrand: "達妯・提庫依神廟",
            support: "收錄薩爾達傳說、大亂鬥特別版等系列的進階攻略與程錯解說。",
            actionsLabel: "入口動作按鈕",
            enter: "進入神廟",
            channel: "造訪頻道",
            footer: "© 2026 Yuhudaddy － 內容整理自個人 YouTube 頻道",
            langLabel: "切換語言",
            brandLink: "回到原版入口",
            toLight: "切換為淺色主題",
            toDark: "切換為深色主題",
          },
          en: {
            htmlLang: "en",
            docTitle: "Yuda's Channel · Game Wiki",
            metaDescription: "Advanced game guides and mechanics for The Legend of Zelda, Super Smash Bros., and beyond.",
            skip: "Skip to main content",
            brand: "Dazotikuy Shrine",
            eyebrowLeft: "Yuda's Channel",
            eyebrowRight: "Game Wiki",
            titleLead: "Welcome to",
            titleBrand: "Dazotikuy Shrine",
            support: "Advanced game guides and mechanics for The Legend of Zelda, Super Smash Bros., and beyond.",
            actionsLabel: "Entrance actions",
            enter: "Enter Shrine",
            channel: "Visit Channel",
            footer: "© 2026 Yuhudaddy - Content Curated from Personal YouTube Channel",
            langLabel: "Change language",
            brandLink: "Back to the original entrance",
            toLight: "Switch to light theme",
            toDark: "Switch to dark theme",
          },
          ja: {
            htmlLang: "ja",
            docTitle: "Yudaのチャンネル・ゲーム攻略Wiki",
            metaDescription: "ゼルダの伝説やスマブラSPなど、上級テクニックとバグ解説をまとめたWikiです。",
            skip: "メインコンテンツへスキップ",
            brand: "ダゾ・チクイ祠",
            eyebrowLeft: "Yudaのチャンネル",
            eyebrowRight: "ゲーム攻略Wiki",
            titleLead: "へようこそ",
            titleBrand: "ダゾ・チクイ祠",
            support: "ゼルダの伝説やスマブラSPなど、上級テクニックとバグ解説をお届けします。",
            actionsLabel: "入口アクション",
            enter: "祠へ",
            channel: "チャンネルへ",
            footer: "© 2026 Yuhudaddy － 個人YouTubeチャンネルより編纂",
            langLabel: "言語を変更",
            brandLink: "元の入口へ戻る",
            toLight: "ライトテーマに切替",
            toDark: "ダークテーマに切替",
          },
        };

        var langSwitch = document.getElementById("entrance-lang-switch");
        var langButton = document.getElementById("entrance-lang-button");
        var langMenu = document.getElementById("entrance-lang-menu");
        var langOptions = langMenu
          ? Array.prototype.slice.call(langMenu.querySelectorAll("[data-lang-option]"))
          : [];

        function currentLang() {
          var value = root.getAttribute("data-lang");
          return I18N[value] ? value : "en";
        }

        function currentTheme() {
          return root.getAttribute("data-theme") === "light" ? "light" : "dark";
        }

        function commitTheme(theme) {
          root.setAttribute("data-theme", theme);
          root.style.colorScheme = theme;
          try { localStorage.setItem("wiki-theme", theme); } catch (error) {}
          syncThemeControl();
          window.dispatchEvent(new CustomEvent("wiki-theme-change", { detail: theme }));
        }

        function setText(selector, value) {
          var node = document.querySelector(selector);
          if (node) node.textContent = value;
        }

        function syncThemeControl() {
          var theme = currentTheme();
          var t = I18N[currentLang()] || I18N.en;
          if (themeButton) {
            themeButton.setAttribute("aria-label", theme === "dark" ? t.toLight : t.toDark);
          }
          if (themeMeta) {
            themeMeta.setAttribute("content", theme === "dark" ? "#0d3d4b" : "#dce5ec");
          }
        }

        function applyLang(lang) {
          var t = I18N[lang] || I18N.en;
          root.setAttribute("data-lang", lang);
          root.setAttribute("lang", t.htmlLang);
          document.title = t.docTitle;
          if (descriptionMeta) descriptionMeta.setAttribute("content", t.metaDescription);

          setText(".skip-link", t.skip);
          setText(".brand-copy", t.brand);

          // Logo 連結的可讀名稱：品牌字樣本身不足以說明「點了會去哪」
          var brandLink = document.querySelector(".ocean-brand");
          if (brandLink) brandLink.setAttribute("aria-label", t.brandLink);
          setText(".hero-title-prefix", t.titleLead);
          setText(".hero-title em", t.titleBrand);
          setText(".hero-support", t.support);
          setText(".ocean-footer p", t.footer);

          var eyebrows = document.querySelectorAll(".hero-eyebrow > span:not(.eyebrow-line)");
          if (eyebrows[0]) eyebrows[0].textContent = t.eyebrowLeft;
          if (eyebrows[1]) eyebrows[1].textContent = t.eyebrowRight;

          var actions = document.querySelector(".hero-actions");
          if (actions) actions.setAttribute("aria-label", t.actionsLabel);
          var actionLabels = document.querySelectorAll(".hero-action > span:first-child");
          if (actionLabels[0]) actionLabels[0].textContent = t.enter;
          if (actionLabels[1]) actionLabels[1].textContent = t.channel;

          if (langButton) langButton.setAttribute("aria-label", t.langLabel);
          langOptions.forEach(function (option) {
            option.setAttribute(
              "aria-checked",
              option.getAttribute("data-lang-option") === lang ? "true" : "false"
            );
          });
          syncThemeControl();
        }

        function closeLangMenu(refocus) {
          if (!langMenu || langMenu.hidden) return;
          langMenu.hidden = true;
          if (langButton) {
            langButton.setAttribute("aria-expanded", "false");
            if (refocus) langButton.focus();
          }
        }

        function openLangMenu(focusFirst) {
          if (!langMenu || langMenu.hidden === false) return;
          langMenu.hidden = false;
          if (langButton) langButton.setAttribute("aria-expanded", "true");
          if (!focusFirst) return;
          var checked = langOptions.filter(function (option) {
            return option.getAttribute("aria-checked") === "true";
          })[0];
          var target = checked || langOptions[0];
          if (target) target.focus();
        }

        if (langButton && langMenu) {
          langButton.addEventListener("click", function (event) {
            event.stopPropagation();
            if (langMenu.hidden) openLangMenu(event.detail === 0);
            else closeLangMenu(false);
          });

          langOptions.forEach(function (option, index) {
            option.addEventListener("click", function () {
              var next = option.getAttribute("data-lang-option");
              applyLang(next);
              try { localStorage.setItem("entrance-lang", next); } catch (error) {}
              closeLangMenu(true);
            });

            option.addEventListener("keydown", function (event) {
              if (event.key !== "ArrowDown" && event.key !== "ArrowUp") return;
              event.preventDefault();
              var step = event.key === "ArrowDown" ? 1 : -1;
              var target = langOptions[(index + step + langOptions.length) % langOptions.length];
              if (target) target.focus();
            });
          });

          document.addEventListener("keydown", function (event) {
            if (event.key === "Escape") closeLangMenu(true);
          });

          document.addEventListener("pointerdown", function (event) {
            if (langSwitch && !langSwitch.contains(event.target)) closeLangMenu(false);
          });
        }

        applyLang(currentLang());

        if (themeButton) {
          themeButton.addEventListener("click", function () {
            var current = currentTheme();
            var next = current === "dark" ? "light" : "dark";
            if (themeButton.disabled) return;

            var reducedMotion = window.matchMedia("(prefers-reduced-motion: reduce)").matches;
            if (reducedMotion) {
              commitTheme(next);
              return;
            }

            var sourceDisc = themeButton.querySelector(current === "dark" ? ".moon-disc" : ".sun-disc");
            var targetDisc = themeButton.querySelector(next === "dark" ? ".moon-disc" : ".sun-disc");
            var glow = themeButton.querySelector(".celestial-glow");
            var fadeTransition = "opacity 260ms cubic-bezier(0.65, 0, 0.35, 1)";
            var revealTransition = "opacity 650ms cubic-bezier(0.65, 0, 0.35, 1)";

            themeButton.disabled = true;
            themeButton.classList.add("is-switching");
            [sourceDisc, targetDisc, glow].forEach(function (node) {
              if (!node) return;
              node.style.transition = fadeTransition;
              node.style.opacity = "0";
            });

            // 先讓目前位置的圖示完整淡出，再讓父層換到另一個主題的位置。
            window.setTimeout(function () {
              commitTheme(next);
              themeButton.classList.add("is-relocating");
              themeButton.classList.remove("is-switching");

              window.requestAnimationFrame(function () {
                if (targetDisc) {
                  targetDisc.style.transition = revealTransition;
                  targetDisc.style.opacity = "1";
                }
                if (glow) {
                  glow.style.transition = revealTransition;
                  glow.style.removeProperty("opacity");
                }

                window.setTimeout(function () {
                  [sourceDisc, targetDisc, glow].forEach(function (node) {
                    if (!node) return;
                    node.style.removeProperty("opacity");
                    node.style.removeProperty("transition");
                  });
                  themeButton.classList.remove("is-relocating");
                  themeButton.disabled = false;
                }, 760);
              });
            }, 320);
          });
          themeButton.addEventListener("pointerup", function () { themeButton.blur(); });
        }

        document.addEventListener("visibilitychange", function () {
          if (page) page.classList.toggle("is-paused", document.hidden);
        });

        var reducedMotion = window.matchMedia("(prefers-reduced-motion: reduce)");
        var finePointer = window.matchMedia("(pointer: fine)");
        var pointerFrame = 0;

        window.addEventListener("pointermove", function (event) {
          if (reducedMotion.matches || !finePointer.matches || pointerFrame) return;
          pointerFrame = window.requestAnimationFrame(function () {
            var x = (event.clientX / window.innerWidth - 0.5) * 2;
            var y = (event.clientY / window.innerHeight - 0.5) * 2;
            root.style.setProperty("--scene-x", (x * 1.5).toFixed(2) + "px");
            root.style.setProperty("--scene-y", (y * 1).toFixed(2) + "px");
            pointerFrame = 0;
          });
        }, { passive: true });
      })();
    </script>
  </body>
</html>
